# Core runtime dependencies (mirrors [project].dependencies in pyproject.toml).
requests>=2.32.4
polars==1.6.0
duckdb==1.0.0

# Optional extras:
#   pip install "intermine314[speed]"      # orjson
#   pip install "intermine314[benchmark]"  # pandas, intermine
#   pip install "intermine314[dev]"        # pytest, ruff
